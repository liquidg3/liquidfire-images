{
    "properties": {
        "uploadDestination": {
            "type":    "path",
            "options": {
                "label":       "Uploads Dir",
                "description": "Where should uploads go?",
                "required":    true
            }
        }
    }
}